# Backlog

Priorities: **P0** — критично, **P1** — важно, **P2** — полезно, **P3** — сложно/экспериментально, **P4** - опционально

## Баги

## Улучшения UX / Навигация

- [ ] `UX-001` · **P3** · **«Сохранить все»** — если в одном сообщении несколько операций, общая кнопка, сохраняющая все валидные черновики разом.
- [ ] `UX-002` · **P3** · **Черновики переживают рестарт** — сейчас они в памяти и теряются при деплое; хранить в `data/`.

## Новые фичи

- [ ] `FEAT-005` · **P4** · **Запланированные операции** - Функционал «Запланировать операцию»:
  - Пользователь указывает дату, время, тип, категорию, сумму и комментарий
  - В нужное время бот присылает уведомление с данными операции
  - Пользователь может: подтвердить (записать в таблицу), отклонить или отредактировать
- [ ] `FEAT-009` · **P3** · **Импорт данных Excel** - Парсинг Excel-таблиц (`.xlsx`) — загрузить файл, распознать транзакции, импортировать в «Операции»
- [ ] `FEAT-011` · **P4** · **Импорт данных text** - Парсинг заметок (plain text / Markdown) с помощью AI — по сути пакетный режим текущего AI-ввода
- [ ] `FEAT-012` · **P4** · **Лимиты и планы по категориям** - Команда для установки лимитов (`/limit Food 15000`):
  - Хранение лимитов (отдельный лист в Google Sheets)
  - Уведомление при 80% и 100% лимита
  - Сравнение факт/план в `/ask` — у аналитика уже есть медианы по категориям, не хватает только цели
- [x] `FEAT-015` · **P4** · **Отчёты в боте** — закрыто командой `/ask` (v2.2): анализ, прогноз и планирование поверх истории «Операции» в USD.
